# AGENTS.md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a tool-agnostic `AGENTS.md` to the sitesnap repository that orients AI agents (Claude Code, Codex CLI, Aider) working on sitesnap itself — file structure, dev commands, conventions, release flow — so future development sessions get instant context regardless of which agent is used.

**Architecture:** A single static markdown file at the repo root. No code changes, no test changes. Excluded from npm pack (the `files` array in `package.json` lists explicit entries; `AGENTS.md` is not added there because end-users don't need it). Stays inside the GitHub repository only.

**Tech Stack:** Markdown only. No new dependencies.

---

## File Structure

**Files created:**
- `AGENTS.md` — tool-agnostic agent guide (~80 lines).

**Files NOT touched:**
- `package.json` `files` array — `AGENTS.md` is intentionally NOT added there. End users installing via npm don't need this file; it's purely for repository contributors.
- README.md / README.en.md — already cover end-user usage (including the `AGENTS.md` snippet for users to paste in their OWN projects). The new repo-level `AGENTS.md` is a different artifact (contributor-facing) and doesn't replace README.
- Tests — nothing testable.

---

## Task 1: Create `AGENTS.md` at repo root

**Files:**
- Create: `AGENTS.md`

- [ ] **Step 1: Verify the facts that will be cited in the file**

Run these and confirm the output matches what's stated in the AGENTS.md draft below. If any drift, update the draft text before writing.

```bash
# Confirm test command works
npm test 2>&1 | tail -3

# Confirm src/ file list matches what's documented
ls src/

# Confirm package.json fields
node -e "const p=require('./package.json'); console.log(p.name, p.version, p.engines)"

# Confirm npm pack contents (AGENTS.md should NOT appear unless we add it)
npm pack --dry-run 2>&1 | grep -E "^(npm notice [0-9]|npm notice total)" | head -15
```

Expected:
- `npm test` shows 42 tests passing
- `src/` contains exactly: `capture.mjs config.mjs meta.mjs rate-limit.mjs sitemap.mjs url-guard.mjs`
- `package.json`: name `@hayashiii/sitesnap`, version `0.2.1`, engines `{ node: '>=22' }`
- `npm pack --dry-run` does NOT list `AGENTS.md` (that's the desired behavior)

- [ ] **Step 2: Write `AGENTS.md`**

Create `/Users/hayashi/Desktop/sitesnap/AGENTS.md` with this exact content:

````markdown
# AGENTS.md — sitesnap repository contributor guide

AIエージェント(Claude Code、Codex CLI、Aider など)が sitesnap リポジトリのコードに手を加える際の指針。

エンドユーザーが sitesnap を**使う**方法は [README.md](./README.md) を参照。本ファイルはこのコードベースを**開発する側**のためのもの。

---

## プロジェクト概要

- npm パッケージ名: `@hayashiii/sitesnap`
- 目的: Web サイトのスクリーンショットを一括キャプチャする CLI(デスクトップ + モバイル)
- ランタイム: Node.js 22+
- ライセンス: MIT
- 主要依存: Playwright、fast-xml-parser

## ファイル構成

| ファイル | 責任 |
|---|---|
| `cli.mjs` | エントリーポイント、argv パース、サブコマンド振り分け |
| `src/sitemap.mjs` | sitemap.xml の取得・解析(再帰ガード、URL検証、UA設定) |
| `src/capture.mjs` | Playwright でのスクリーンショット撮影、cross-origin 警告、rate limiter 統合 |
| `src/meta.mjs` | meta.json と index.json の生成、ページタイトル取得 |
| `src/url-guard.mjs` | SSRF 対策(プライベート IP・非 http スキーム拒否) |
| `src/rate-limit.mjs` | 同一ホストへの最小間隔制御 |
| `src/config.mjs` | `VERSION`、`USER_AGENT`、デフォルト値 |
| `tests/*.test.mjs` | Node.js 標準テストランナー(`node:test`)を使ったユニット/統合テスト |
| `docs/plans/` | 実装計画(機能ごとに 1 ファイル) |
| `docs/specs/` | ブレインストーミング後の設計仕様 |

## 開発コマンド

```bash
# テスト実行(必ず全件パス確認)
npm test

# 開発時の動作確認
node cli.mjs help
node cli.mjs --version
node cli.mjs site <sitemap-url> --limit 3

# npm publish 前の最終チェック
npm pack --dry-run
```

## コーディング方針

- **コメントは「why が非自明」な時のみ書く**。「what」を書かない(コードが自明に語る)
- **新規依存パッケージを増やさない**。今ある 2 つ(Playwright、fast-xml-parser)で済ませる
- **TDD**: 失敗テスト → 実装 → パス確認 → コミットの順
- **ファイルの単一責任**: `src/` の各モジュールはこれを守る。新機能で巨大化させない
- **コミットプレフィックス**: `feat:`、`fix:`、`chore:`、`docs:`、`test:`、`i18n:`、`refactor:` を使う
- **ユーザー向け文字列は日本語**(v0.2.1〜)。技術ログ(`[desktop] 1/3 ok`)は英語のまま視認性優先
- **エラーメッセージはアクション可能**: 何が起きたか + どう直せるか をワンセットで

## やってはいけないこと

- `node_modules/`、`sites/`、`note-draft.md` をコミットしない(全て `.gitignore` 対象)
- `main` ブランチで直接作業しない。`feat/v0.x.y` ブランチを切ってマージする
- `npm publish` を AI エージェントが実行しない。**ユーザーが手動で行う**
- 既存テストの正規表現マッチを無断で緩めない(バグを隠す可能性がある)
- npm pack に含めるべきでないファイル(plans、specs、tests、docs)を `package.json` の `files` 配列に追加しない

## リリース手順

1. `feat/v0.x.y` ブランチを切る
2. 実装 + 関連テストを追加
3. `npm test` で全件パス確認
4. `CHANGELOG.md` に新バージョンエントリ追加(`[Breaking Changes]` を含む場合は明記)
5. `package.json` の `version` を更新
6. main にマージしてプッシュ
7. `git tag v0.x.y && git push origin v0.x.y`
8. ユーザーが `npm publish --access public` を手動実行

## セキュリティ

- SSRF 対策: `src/url-guard.mjs` の `assertPublicUrl()` を**必ず**外部 URL のフェッチ前に呼ぶ。新しい fetch コードを書くときも同様
- ユーザー入力(URL)を直接 `path.join()` に渡さない。`slugify()` を経由する
- `--allow-private` フラグでガードを無効化する場合のみ、プライベート IP へのアクセスを許可

## 参考リンク

- npm: https://www.npmjs.com/package/@hayashiii/sitesnap
- GitHub: https://github.com/hayashiii-ghub/sitesnap
- Issue: https://github.com/hayashiii-ghub/sitesnap/issues
````

- [ ] **Step 3: Verify the file renders correctly**

Run:
```bash
head -5 AGENTS.md
wc -l AGENTS.md
```

Expected: 80-100 lines, header `# AGENTS.md — sitesnap repository contributor guide` is on line 1.

- [ ] **Step 4: Verify `npm pack --dry-run` still excludes AGENTS.md**

Run: `npm pack --dry-run 2>&1 | grep -E "AGENTS|^npm notice total"`
Expected: NO line mentioning `AGENTS.md` (only `npm notice total files: 12`). The `files` array in `package.json` is the allow-list, and we did not add `AGENTS.md` to it.

- [ ] **Step 5: Commit**

```bash
git add AGENTS.md
git commit -m "$(cat <<'COMMIT_MSG'
docs: add AGENTS.md for AI agents contributing to sitesnap

Tool-agnostic guide that orients Claude Code, Codex CLI, Aider, and
similar agents working on the sitesnap codebase. Covers file structure,
dev commands, coding conventions, release flow, and security guardrails.

Excluded from npm pack (kept in the source repo only) so end users
unaffected.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
COMMIT_MSG
)"
```

- [ ] **Step 6: Push to GitHub**

Run: `git push origin main`
Expected: push succeeds. `AGENTS.md` appears at https://github.com/hayashiii-ghub/sitesnap/blob/main/AGENTS.md

---

## Done criteria

- [ ] `AGENTS.md` exists at repo root with the exact content above.
- [ ] `npm test` still passes (no test changes — sanity check).
- [ ] `npm pack --dry-run` total file count is **still 12** (AGENTS.md not added to npm pack).
- [ ] Commit pushed to `origin/main`; GitHub renders the file.
